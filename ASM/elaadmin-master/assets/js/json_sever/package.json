{
  "name": "json_sever",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start-db": "json-server --watch db.json --port 3000",
    "start": "json-server --watch db_product.json --port 4000",
    "start-both": "npm-run-all --parallel start start-db",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4",
    "mysql": "^2.18.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.1",
    "npm-run-all": "^4.1.5"
  }
}
